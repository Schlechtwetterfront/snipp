<UserControl x:Class="clipman.Views.ClipList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:clipman.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <ListBox x:Name="clipList"
             Grid.Row="1"
             Margin="5,5,5,5"
             BorderThickness="0"
             AlternationCount="{Binding Items.Count, RelativeSource={RelativeSource Self}}"
             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
             ItemsSource="{Binding Path=ClipView}"
             >
        <ListBox.ItemTemplate>
            <DataTemplate>
                <StackPanel>
                    <Label Grid.ColumnSpan="4" FontWeight="Bold" FontSize="16" Content="{Binding Title}"/>
                    <StackPanel Orientation="Horizontal">
                        <Label x:Name="created" Grid.Row="1" Grid.Column="1" Content="{Binding CreatedString}"/>
                        <Label x:Name="source" Grid.Row="1" Grid.Column="3">...</Label>
                    </StackPanel>
                </StackPanel>
            </DataTemplate>
        </ListBox.ItemTemplate>
        <ListBox.ItemContainerStyle>
            <Style TargetType="ListBoxItem">
                   <EventSetter Event="MouseDoubleClick" Handler="Copy"></EventSetter>
            </Style>
        </ListBox.ItemContainerStyle>
    </ListBox>
</UserControl>
